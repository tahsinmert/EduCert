import { course } from "./course";
import { certificateTemplate } from "./certificateTemplate";
import { issuedCertificate } from "./issuedCertificate";

export const schemaTypes = [course, certificateTemplate, issuedCertificate];

